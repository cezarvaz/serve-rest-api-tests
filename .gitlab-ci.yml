default:
  tags:
    - aws-linux
stages:
  - test
test:
  image: node:16.17.0-alpine3.16
  stage: test
  script:
    - yarn install --frozen-lockfile
    - yarn test
  artifacts:
    expire_in: 1 week
    when: always
    paths:
      - html-report
